#include <stdio.h>
#include "../common/utils.h"

int main(int argc, char const *argv[]) {
    struct Request package;
    printf("%lu\n", sizeof(package));

    // void* p = (void *)&package;

    return 0;
}
